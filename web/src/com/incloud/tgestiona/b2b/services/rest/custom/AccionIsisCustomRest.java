package com.incloud.tgestiona.b2b.services.rest.custom;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.incloud.tgestiona.b2b.services.rest.AccionIsisRest;

@RestController
@RequestMapping("/api/accionIsis")
public class AccionIsisCustomRest extends AccionIsisRest {

	private final Logger log = LoggerFactory.getLogger(AccionIsisCustomRest.class);
	
	/*
	 * methods personalize
	 */

	
}
